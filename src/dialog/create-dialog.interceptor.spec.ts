import { CreateDialogInterceptor } from './create-dialog.interceptor';

describe('CreateDialogInterceptor', () => {
  it('should be defined', () => {
    expect(new CreateDialogInterceptor()).toBeDefined();
  });
});
