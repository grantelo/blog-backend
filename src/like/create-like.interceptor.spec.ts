import { CreateLikeInterceptor } from './create-like.interceptor';

describe('CreateLikeInterceptor', () => {
  it('should be defined', () => {
    expect(new CreateLikeInterceptor()).toBeDefined();
  });
});
